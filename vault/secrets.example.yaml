# =============================================================================
# LinkinClaw Vault — Your Private Credentials
# =============================================================================
# Copy this file to secrets.yaml and fill in your real values:
#   cp secrets.example.yaml secrets.yaml
#
# ⚠️ secrets.yaml is gitignored. Never commit it.
# =============================================================================

# LinkedIn Login
linkedin:
  email: "your-linkedin-email@example.com"
  password: "your-linkedin-password"

# Your profile info (used to fill application forms)
user_profile:
  name: "Your Full Name"
  email: "your-email@example.com"
  phone: "+1(555)123-4567"
  linkedin_url: "https://www.linkedin.com/in/your-profile"
  website: ""  # Optional: portfolio/personal site
  sponsorship_answer: "No"  # "Yes" if you need visa sponsorship, "No" if not
